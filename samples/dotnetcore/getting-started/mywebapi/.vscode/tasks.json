{
    "tasks": [
        {
            "label": "build (VSCE)",
            "command": "vsce",
            "args": [
                "up",
                "--iterate",
                "--no-command",
                "--no-telemetry"
            ],
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "problemMatcher": "$msCompile"
        }
    ]
}